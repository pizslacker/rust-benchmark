# rust-benchmark

C benchmark program showcasing (fully optimized) C performance compared to idiomatic Rust.

This was actually an excersize for me in compiling a Rust library (_librustsum.a_) and linking it into the `rust-benchmark` C program with GCC.
